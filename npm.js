// npm - global command, comes with node
// npm --version

// locl dependency - use it only in this particular project
// npm i <packageName>

// global dependency - use it in any prohect
// npm install -g <packageName>
// sudo install -f <packageName> (mac)

// package.json - manifest file (stores improtant info about the app)
// manual approach
// npm init (step by step and press ener to skip)
// npm init -y (everything will be set to default)
